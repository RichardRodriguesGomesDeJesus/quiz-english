import styled from "styled-components";

const ContainerQuesion = styled.div`
    display: flex;
    flex-direction: column;
`

export default function Question(){
    return(
        <ContainerQuesion>

        </ContainerQuesion>
    );
}